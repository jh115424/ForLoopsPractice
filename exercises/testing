 function getClientWithLeastPositiveBalance(array) {
  // Your code goes here...

let lowestBalance = [1];
let balance = []


  for (let i = 0; i < array.length; i++) {
if(array[i].balance > 0) {
  let currentBalance = array[i].balance;


    }
  }
return lowestBalance;
  } 




 function getClientWithLeastPositiveBalance(array) {
    // Your code goes here...
  
  
  let lowestBalance2 = array[0];
  let lowestBalance = []
  
  
    for (let i = 0; i < array.length; i++) {
      if(array[i] > lowestBalance && array[i].balance > 0) {
        lowestBalance.push(array[i].array);
  } 
  
      }
  return lowestBalance;
    } 



   