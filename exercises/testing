function getAllClientNames(array) {

  const names = [
    'Susan',
    'Morgan',
    'Joshua',
    'Candy',
    'Phil',
  ];

  let namesArray = [0];

return namesArray.map((person) => person.name);


}

console.log(getAllClientNames(names));
